import { FilterThemesPipe } from './filter-themes.pipe';

describe('FilterThemesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterThemesPipe();
    expect(pipe).toBeTruthy();
  });
});

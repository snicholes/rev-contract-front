import { SortRubricPipe } from './sort-rubric.pipe';

describe('SortRubricPipe', () => {
  it('create an instance', () => {
    const pipe = new SortRubricPipe();
    expect(pipe).toBeTruthy();
  });
});

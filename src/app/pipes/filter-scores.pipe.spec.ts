import { FilterScoresPipe } from './filter-scores.pipe';

describe('FilterScoresPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterScoresPipe();
    expect(pipe).toBeTruthy();
  });
});
